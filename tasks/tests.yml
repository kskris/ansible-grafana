---

- name: Check if grafana is running with set up credentials
  uri:
    url: "http://{{ grafana_address }}:{{ grafana_port }}"
    user: "{{ grafana_admin }}"
    password: "{{ grafana_password }}"
    force_basic_auth: yes
